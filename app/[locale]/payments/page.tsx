'use client';
import {useEffect,useState} from 'react';
export default function Payments(){const [rows,setRows]=useState<any[]>([]);const [assessments,setAssessments]=useState<any[]>([]);const [form,setForm]=useState<any>({assessment_id:'',payment_date:new Date().toISOString().slice(0,10),amount:'',currency:'SAR',base_amount:'',beneficiary:'',reference:''});const load=()=>{fetch('/api/payments').then(r=>r.ok?r.json():[]).then(setRows);fetch('/api/assessments').then(r=>r.ok?r.json():[]).then(a=>{setAssessments(a);if(a[0]&&!form.assessment_id)setForm((x:any)=>({...x,assessment_id:a[0].id,currency:a[0].currency}))})};useEffect(load,[]);async function save(){const r=await fetch('/api/payments',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...form,amount:Number(form.amount),base_amount:Number(form.base_amount||form.amount)})});if(r.ok)load()}return <main className="container"><h1>مدفوعات الزكاة</h1><div className="card section"><div className="form-grid"><label>الاحتساب<select value={form.assessment_id} onChange={e=>setForm({...form,assessment_id:e.target.value})}>{assessments.map(a=><option key={a.id} value={a.id}>{a.assessment_date} — {a.zakat_due} {a.currency}</option>)}</select></label><label>التاريخ<input type="date" value={form.payment_date} onChange={e=>setForm({...form,payment_date:e.target.value})}/></label><label>المبلغ<input type="number" step="any" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value,base_amount:e.target.value})}/></label><label>المستفيد<input value={form.beneficiary} onChange={e=>setForm({...form,beneficiary:e.target.value})}/></label><label>المرجع<input value={form.reference} onChange={e=>setForm({...form,reference:e.target.value})}/></label></div><button className="btn" onClick={save} disabled={!form.assessment_id}>تسجيل الدفع</button></div><div className="card section"><table className="table"><thead><tr><th>التاريخ</th><th>المبلغ</th><th>العملة</th><th>المستفيد</th><th>المرجع</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.payment_date}</td><td>{r.amount}</td><td>{r.currency}</td><td>{r.beneficiary||'—'}</td><td>{r.reference||'—'}</td></tr>)}</tbody></table></div></main>}

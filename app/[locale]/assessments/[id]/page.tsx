'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';
export default function AssessmentDetail({params}:{params:{locale:string,id:string}}){const ar=params.locale==='ar';const [x,setX]=useState<any>();const [msg,setMsg]=useState('');useEffect(()=>{fetch(`/api/assessments/${params.id}`).then(r=>r.json()).then(setX)},[params.id]);async function confirm(){const r=await fetch('/api/assessments/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:params.id})});const j=await r.json();setMsg(r.ok?(ar?'تم اعتماد الاحتساب':'Assessment confirmed'):j.error);if(r.ok)setX({...x,assessment:j})}if(!x)return <main className="container">Loading…</main>;const a=x.assessment||x;return <main className="container"><div className="page-head"><div><h1>{ar?'تفاصيل احتساب الزكاة':'Zakat Assessment'}</h1><p className="muted">{a.id}</p></div><div><Link className="btn secondary" href={`/${params.locale}/reports`}>{ar?'التقارير':'Reports'}</Link>{a.status==='CALCULATED'&&<button className="btn" style={{marginInlineStart:8}} onClick={confirm}>{ar?'اعتماد':'Confirm'}</button>}</div></div>{msg&&<div className="success">{msg}</div>}<div className="grid section"><Card t="Assessment Date" v={a.assessment_date}/><Card t="Valuation Date" v={a.valuation_date}/><Card t="Zakatable Value" v={a.total_zakatable_value}/><Card t="Zakat Due" v={`${a.zakat_due} ${a.currency}`}/></div><section className="card section"><h3>{ar?'لماذا هذا المبلغ؟':'Why this amount?'}</h3><table className="table"><thead><tr><th>Asset / Lot</th><th>Market Value</th><th>Eligible</th><th>Zakat</th><th>Status</th><th>Explanation</th></tr></thead><tbody>{(x.lines||[]).map((l:any)=><tr key={l.id}><td>{l.lot_id}</td><td>{l.market_value}</td><td>{l.eligible_value}</td><td>{l.zakat_amount}</td><td><span className="pill">{l.eligibility_status}</span></td><td>{l.explanation}</td></tr>)}</tbody></table></section><section className="card section"><h3>Snapshot</h3><pre className="pre">{JSON.stringify(a.calculation_snapshot,null,2)}</pre></section></main>}
function Card({t,v}:{t:string,v:any}){return <div className="card"><div className="muted">{t}</div><div className="metric">{v||'—'}</div></div>}

'use client';
import {useState} from 'react'; import {supabaseBrowser} from '@/lib/supabase/browser';
export default function Login(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');async function login(){const supabase=supabaseBrowser();const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback?next=/ar/dashboard`}});setMsg(error?.message||'تم إرسال رابط الدخول إلى بريدك.')}return <main className="container" style={{maxWidth:520}}><div className="card hero"><h1>تسجيل الدخول</h1><p className="muted">أدخل بريدك الإلكتروني لإرسال رابط دخول آمن.</p><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="email@example.com" style={{width:'100%',padding:12,margin:'12px 0'}}/><button className="btn" onClick={login}>إرسال رابط الدخول</button>{msg&&<p className="muted">{msg}</p>}</div></main>}

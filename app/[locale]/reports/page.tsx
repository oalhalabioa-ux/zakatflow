'use client';
import Link from 'next/link';
export default function Reports({params}:{params:{locale:string}}){const ar=params.locale==='ar';async function download(type:string){const r=await fetch(`/api/reports?type=${type}`);if(!r.ok)return;const blob=await r.blob();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`zakatflow-${type}.csv`;a.click();URL.revokeObjectURL(url)}return <main className="container"><div className="page-head"><div><h1>{ar?'التقارير':'Reports'}</h1><p className="muted">{ar?'تقارير تشغيلية قابلة للتدقيق والتصدير.':'Auditable operational reports with CSV export.'}</p></div><Link className="btn secondary" href={`/${params.locale}/reports/print`}>{ar?'تقرير للطباعة':'Printable report'}</Link></div><div className="cards section">{[['assessments',ar?'الاحتسابات':'Assessments'],['transactions',ar?'المعاملات':'Transactions'],['lots','Lots'],['payments',ar?'المدفوعات':'Payments']].map(([t,l])=><div className="card" key={t}><h3>{l}</h3><button className="btn secondary" onClick={()=>download(t as string)}>CSV</button></div>)}</div></main>}

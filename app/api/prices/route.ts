import {NextResponse} from 'next/server';import {requireUser} from '@/services/auth';import {priceSchema} from '@/lib/validation/schemas';
export async function GET(req:Request){try{const {supabase}=await requireUser();const {searchParams}=new URL(req.url);let q=supabase.from('market_prices').select('*').order('valuation_date',{ascending:false}).order('created_at',{ascending:false}).limit(200);if(searchParams.get('asset_type'))q=q.eq('asset_type',searchParams.get('asset_type')!);const {data,error}=await q;if(error)throw error;return NextResponse.json(data??[])}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
export async function POST(req:Request){try{const {supabase,user}=await requireUser();const p=priceSchema.parse(await req.json());const {data,error}=await supabase.from('market_prices').insert(p).select().single();if(error)throw error;await supabase.from('audit_logs').insert({user_id:user.id,entity_type:'market_price',entity_id:data.id,action:'CREATE',new_data:data});return NextResponse.json(data,{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}

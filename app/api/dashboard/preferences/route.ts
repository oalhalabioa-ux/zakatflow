import {NextResponse} from 'next/server';
import {requireUser} from '@/services/auth';
const allowed=new Set(['assets','gain','zakatable','due','paid','remaining','gold','cycle','portfolio','status','payments']);
export async function GET(){try{const{supabase,user}=await requireUser();const{data,error}=await supabase.from('dashboard_preferences').select('preset,widgets').eq('user_id',user.id).maybeSingle();if(error)throw error;return NextResponse.json(data||null)}catch(e:any){return NextResponse.json({error:e.message},{status:401})}}
export async function PUT(req:Request){try{const body=await req.json(),preset=String(body.preset||'custom'),widgets=Array.isArray(body.widgets)?body.widgets.map(String).filter((x:string)=>allowed.has(x)):[];if(!widgets.length)return NextResponse.json({error:'At least one dashboard widget is required'},{status:400});const{supabase,user}=await requireUser();const{data,error}=await supabase.from('dashboard_preferences').upsert({user_id:user.id,preset,widgets,updated_at:new Date().toISOString()},{onConflict:'user_id'}).select('preset,widgets').single();if(error)throw error;return NextResponse.json(data)}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}

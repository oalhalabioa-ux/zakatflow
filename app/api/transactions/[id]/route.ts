import {NextResponse} from 'next/server';
import {requireUser} from '@/services/auth';import {reverseTransaction} from '@/services/transactions';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const {supabase,user}=await requireUser();const {data,error}=await supabase.from('transactions').select('*,asset_accounts(name,asset_type,currency),transaction_allocations(id,lot_id,quantity,value_base,allocation_method,lots(acquisition_date,hawl_start_date,hawl_due_date,status))').eq('id',id).eq('user_id',user.id).single();if(error)throw error;return NextResponse.json(data)}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:404})}}
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const body=await req.json();if(body.action!=='reverse')return NextResponse.json({error:'INVALID_ACTION'},{status:400});const result=await reverseTransaction(id);return NextResponse.json({id:result,status:'REVERSED'},{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}

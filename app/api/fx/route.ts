import {NextResponse} from 'next/server';import {requireUser} from '@/services/auth';import {fxSchema} from '@/lib/validation/schemas';
export async function GET(){try{const {supabase}=await requireUser();const {data,error}=await supabase.from('fx_rates').select('*').order('valuation_date',{ascending:false}).limit(300);if(error)throw error;return NextResponse.json(data??[])}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
export async function POST(req:Request){try{const {supabase,user}=await requireUser();const p=fxSchema.parse(await req.json());const {data,error}=await supabase.from('fx_rates').upsert(p,{onConflict:'from_currency,to_currency,valuation_date,source'}).select().single();if(error)throw error;await supabase.from('audit_logs').insert({user_id:user.id,entity_type:'fx_rate',entity_id:data.id,action:'UPSERT',new_data:data});return NextResponse.json(data,{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}

import { NextResponse } from 'next/server'; import { requireUser } from '@/services/auth'; import { transferSchema } from '@/lib/validation/schemas';
export async function POST(req:Request){try{const p=transferSchema.parse(await req.json());const {supabase,user}=await requireUser();const {data,error}=await supabase.rpc('create_internal_transfer',{p_user_id:user.id,p_source_account:p.source_asset_account_id,p_destination_account:p.destination_asset_account_id,p_date:p.transfer_date,p_quantity:p.quantity,p_value:p.value,p_currency:p.currency,p_notes:p.notes??null});if(error)throw error;return NextResponse.json(data,{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}

'use client';
export default function ZakatCycleJourney({locale,stage='assessment'}:{locale:string,stage?:'assessment'|'payment'}){const ar=locale==='ar';const steps=ar?['بلوغ النصاب','بدء الحول','احتساب تقديري','الاستحقاق','احتساب نهائي','السداد','إغلاق الدورة']:['Nisab reached','Hawl starts','Estimate','Due date','Final assessment','Payment','Cycle close'];const active=stage==='assessment'?2:5;return <section className="card section cycle-journey"><div className="cycle-journey-head"><div><small className="muted">ZAKAT CYCLE JOURNEY</small><h3>{ar?'مسار الدورة الزكوية':'Zakat cycle journey'}</h3></div><span className="pill">{ar?'دورة واحدة • احتسابات متعددة • التزام واحد':'One cycle • multiple assessments • one obligation'}</span></div><div className="cycle-journey-steps">{steps.map((s,i)=><div key={s} className={i<active?'done':i===active?'active':''}><span>{String(i+1).padStart(2,'0')}</span><strong>{s}</strong></div>)}</div></section>}

'use client';
import {useEffect,useState} from 'react';
import {usePathname} from 'next/navigation';
const COLS=[['asset','الأصل','Asset'],['date','تاريخ الشراء','Purchase date'],['weight','الوزن','Weight'],['hawl','الحول','Hawl'],['cost','التكلفة','Cost'],['current','القيمة الحالية','Current value'],['due','الزكاة المستحقة','Zakat due'],['paid','المدفوعة','Paid'],['remaining','المتبقي','Remaining'],['calculation','الاحتساب الزكوي','Zakat calculation'],['action','الإجراء','Action']] as const;
type Size='compact'|'normal'|'wide';const defaults=()=>Object.fromEntries(COLS.map(c=>[c[0],true])) as Record<string,boolean>;
export default function AssetTableTools({ar}:{ar:boolean}){const path=usePathname();const active=path?.includes('/assets');const[open,setOpen]=useState(false),[deleteOpen,setDeleteOpen]=useState(false),[size,setSize]=useState<Size>('normal'),[visible,setVisible]=useState<Record<string,boolean>>(defaults),[assets,setAssets]=useState<any[]>([]),[selected,setSelected]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);
 useEffect(()=>{if(!active)return;try{const s=localStorage.getItem('zf_asset_table_v2');if(s){const p=JSON.parse(s);if(['compact','normal','wide'].includes(p.size))setSize(p.size);if(p.visible)setVisible({...defaults(),...p.visible})}}catch{}},[active]);useEffect(()=>{if(!active)return;try{localStorage.setItem('zf_asset_table_v2',JSON.stringify({size,visible}))}catch{}},[active,size,visible]);if(!active)return null;const all=COLS.every(c=>visible[c[0]]!==false);const apply=(next:Record<string,boolean>)=>{setVisible(next);window.dispatchEvent(new CustomEvent('zf-asset-table-settings',{detail:{size,visible:next}}))};const applySize=(next:Size)=>{setSize(next);window.dispatchEvent(new CustomEvent('zf-asset-table-settings',{detail:{size:next,visible}}))};
 const openDelete=async()=>{setDeleteOpen(x=>!x);setMsg('');if(!deleteOpen){const r=await fetch('/api/assets');if(r.ok)setAssets(await r.json())}};
 const remove=async()=>{if(!selected)return;const a=assets.find(x=>x.id===selected);if(!confirm(ar?`حذف الأصل «${a?.name||''}»؟ سيتم الرفض تلقائياً إذا كان مرتبطاً بتاريخ مالي.`:`Delete “${a?.name||''}”? It will be blocked automatically if linked to financial history.`))return;setBusy(true);const r=await fetch(`/api/assets/${selected}`,{method:'DELETE'}),j=await r.json().catch(()=>({}));setBusy(false);if(r.ok){setAssets(x=>x.filter(a=>a.id!==selected));setSelected('');setMsg(ar?'تم حذف الأصل غير المرتبط بأمان. أعد تحميل الصفحة لتحديث التقرير.':'Unused asset deleted safely. Reload to refresh the report.');window.location.reload()}else if(j.error==='ASSET_PROTECTED'){const l=j.linked||{};setMsg(ar?`لا يمكن حذف الأصل لأنه مرتبط بسجل مالي: دفعات أصل ${l.lots||0}، حركات ${l.transactions||0}، تحويلات ${l.transfers||0}، تخصيصات زكاة ${l.allocations||0}.`:`Asset is protected: lots ${l.lots||0}, transactions ${l.transactions||0}, transfers ${l.transfers||0}, Zakat allocations ${l.allocations||0}.`)}else setMsg(j.error||(ar?'تعذر الحذف':'Delete failed'))};
 return <><style>{`.asset-table-tools{max-width:1240px;margin:14px auto -10px;padding:0 22px;position:relative}.att-bar{display:flex;gap:8px;align-items:center;justify-content:flex-end;flex-wrap:wrap}.att-panel{position:absolute;z-index:25;top:46px;inset-inline-end:22px;width:min(470px,calc(100vw - 44px));background:#fff;border:1px solid var(--line);border-radius:14px;padding:15px;box-shadow:0 14px 35px #0f231e20}.att-panel h4{margin:0 0 11px}.att-cols{display:grid;grid-template-columns:1fr 1fr;gap:7px}.att-cols label{display:flex;align-items:center;gap:7px;padding:8px;border:1px solid var(--line);border-radius:8px;color:var(--ink)}.att-cols input{width:auto;margin:0}.att-sizes{display:flex;gap:6px;margin-bottom:12px}.att-sizes button{flex:1}.asset-report-table{table-layout:auto;width:max-content;min-width:100%}.asset-report-table th{position:relative;resize:horizontal;overflow:auto;min-width:72px;max-width:520px;cursor:col-resize}.asset-report-table td{cursor:default}.asset-report-table th:first-child{min-width:150px}.asset-report-table th:last-child,.asset-report-table td:last-child{width:72px!important;min-width:72px!important;max-width:82px!important;padding-left:5px!important;padding-right:5px!important;text-align:center;white-space:nowrap}.asset-report-table td:last-child .btn{padding:5px 7px!important;font-size:11px!important;min-width:0!important}.asset-report-table th::-webkit-resizer{background:transparent}@media(max-width:600px){.att-cols{grid-template-columns:1fr}}`}</style><div className="asset-table-tools"><div className="att-bar"><button className="btn secondary" onClick={()=>applySize(size==='compact'?'normal':size==='normal'?'wide':'compact')}>↔ {ar?'حجم الخانات':'Cell size'}: {size==='compact'?(ar?'صغير':'Compact'):size==='wide'?(ar?'واسع':'Wide'):(ar?'متوسط':'Normal')}</button><button className="btn secondary" onClick={()=>setOpen(x=>!x)}>☷ {ar?'اختيار الأعمدة':'Choose columns'}</button><button className="btn secondary" onClick={openDelete}>🗑 {ar?'حذف أصل':'Delete asset'}</button></div>{open&&<div className="att-panel"><h4>{ar?'تخصيص جدول الأصول':'Customize asset table'}</h4><div className="att-sizes"><button className={`btn ${size==='compact'?'':'secondary'}`} onClick={()=>applySize('compact')}>{ar?'صغير':'Compact'}</button><button className={`btn ${size==='normal'?'':'secondary'}`} onClick={()=>applySize('normal')}>{ar?'متوسط':'Normal'}</button><button className={`btn ${size==='wide'?'':'secondary'}`} onClick={()=>applySize('wide')}>{ar?'واسع':'Wide'}</button></div><div className="att-cols">{COLS.map(c=><label key={c[0]}><input type="checkbox" checked={visible[c[0]]!==false} onChange={e=>apply({...visible,[c[0]]:e.target.checked})}/>{ar?c[1]:c[2]}</label>)}</div><div style={{display:'flex',gap:7,marginTop:12}}><button className="btn secondary" onClick={()=>apply(Object.fromEntries(COLS.map(c=>[c[0],!all])))}>{all?(ar?'إخفاء الكل':'Hide all'):(ar?'إظهار الكل':'Show all')}</button><button className="btn" onClick={()=>setOpen(false)}>{ar?'تم':'Done'}</button></div></div>}{deleteOpen&&<div className="att-panel" style={{top:92}}><h4>{ar?'حذف أصل غير مستخدم':'Delete unused asset'}</h4><p className="muted">{ar?'لن يسمح النظام بحذف أصل مرتبط بدفعة أصل أو حركة أو تحويل أو سداد زكاة.':'Assets linked to lots, transactions, transfers or Zakat allocations are protected.'}</p><select value={selected} onChange={e=>setSelected(e.target.value)}><option value="">{ar?'اختر الأصل':'Select asset'}</option>{assets.map(a=><option key={a.id} value={a.id}>{a.name} — {a.asset_type}</option>)}</select><div style={{display:'flex',gap:7,marginTop:10}}><button className="btn" disabled={!selected||busy} onClick={remove}>{busy?'…':ar?'حذف آمن':'Safe delete'}</button><button className="btn secondary" onClick={()=>setDeleteOpen(false)}>{ar?'إغلاق':'Close'}</button></div>{msg&&<p className="muted">{msg}</p>}</div>}</div></>}

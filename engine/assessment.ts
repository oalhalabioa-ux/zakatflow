import Decimal from 'decimal.js';
export interface AssessmentLot {lotId:string;marketValueBase:Decimal.Value;hawlCompleted:boolean;}
export function calculateAssessment(lots:AssessmentLot[],nisabValueBase:Decimal.Value,rate:Decimal.Value=new Decimal('0.025')){const nisab=new Decimal(nisabValueBase);const r=new Decimal(rate);const lines=lots.map(l=>{const market=new Decimal(l.marketValueBase);return {lotId:l.lotId,marketValueBase:market,eligibleValue:l.hawlCompleted?market:new Decimal(0),zakatAmount:new Decimal(0),status:l.hawlCompleted?'ELIGIBLE':'HAWL_NOT_COMPLETED',explanation:l.hawlCompleted?'Hawl completed under selected ruleset.':'Hawl has not completed under selected ruleset.'}});const total=lines.reduce((s,l)=>s.add(l.eligibleValue),new Decimal(0));const qualifies=total.gte(nisab);for(const l of lines){if(qualifies)l.zakatAmount=l.eligibleValue.mul(r);else if(l.status==='ELIGIBLE'){l.status='BELOW_NISAB';l.explanation='Total eligible wealth is below the selected Nisab.';}}return {nisabValueBase:nisab,totalEligibleValue:qualifies?total:new Decimal(0),rate:r,zakatDue:qualifies?total.mul(r):new Decimal(0),lines};}
